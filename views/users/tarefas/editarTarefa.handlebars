{{#each erros}}
    <div class="alert alert-danger">{{texto}}</div>
{{else}}

{{/each}}

<h2 id="nomeFuncionario">Editar tarefa</h2>
 
     
<div class="card mt-4">
    <div class="card-body">
        <label for="nome" style="opacity: 0.5;">*Campos de preenchimento obrigatório</label>
        <form action="" method="POST">
            <input type="hidden" name="id" id="id" value="{{tarefa._id}}"> 

            {{#ifUR}}
            <label for="nome">Nome*</label>
            <input type="text" id="nome" name="nome" value="{{tarefa.nome}}" class="form-control mb-4" required>
            
            <label for="descricao">Descrição*</label>
            <textarea class="form-control mb-4" name="descricao" id="exampleFormControlTextarea1" rows="3" placeholder="Descrição da tarefa" required>{{tarefa.descricao}}</textarea>

            <div class="form-group">
                <label for="funcionarios">Funcionarios a associar</label>
                <select multiple class="form-control" id="funcionarios" name="funcionarios">
                    {{#each funcionarios}}
                        <option value="{{nome}}">{{nome}}</option>
                    {{/each}}
                </select>
            </div>
            {{else}}
                <label for="nome">Nome: {{tarefa.nome}}</label>
                <br>
                
                <label for="descricao" class="mt-3 mb-4">Descrição: {{tarefa.descricao}}</label>
                <br>
                
            {{/ifUR}}
            <label for="dataPrevistaInicio">Data prevista para inicio</label>
            <div class="row">
                <div class="col-3">
                    <input type="datetime-local" value="{{dataPrevistaInicio}}" name="dataPrevistaInicio" id="dataPrevistaInicio" class="form-control mb-4">
                </div>
            </div>
            
            <label for="dataPrevistaFim">Data prevista para fim</label>
            <div class="row">
                <div class="col-3">
                    <input type="datetime-local" value="{{dataPrevistaFim}}" name="dataPrevistaFim" id="dataPrevistaFim" class="form-control mb-4">
                </div>
            </div>

            {{#ifUR}}
            <div class="form-group">
                <label for="importancia">Importância da tarefa</label>
                <select class="form-control" id="importancia" name="importancia">
                    <option>Baixa</option>
                    <option>Média</option>
                    <option>Alta</option>
                </select>
            </div>
            {{else}}
            
                <label for="importancia">Importancia: {{tarefa.importancia}}</label>
                <br>
            {{/ifUR}}
            
            <button type="submit" class="btn btn-dark mt-2">Concluir</button>
        </form>
    </div>
</div>

<script>
</script>